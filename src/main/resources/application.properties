spring.application.name=java_apirest_2026
spring.datasource.url=jdbc:mysql://localhost:3306/mydb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Geocoding service URL template (use {q} for the address query)
# Example: https://api-gouv.lab.rioc.fr/geocodage?q={q}
geocoding.url-template=https://api-gouv.lab.rioc.fr/search?q={q}

# JWT configuration (dev defaults; override via env vars in production)
jwt.secret=changeitchangeitchangeitchangeitchangeitchangeitchangeit12
jwt.expiration-ms=3600000
